#include "main.h"
/**
*_puts_recursion-print a string,followed by new line
*@s:string to print
*Return : Always 0
*/
void _puts_recursion(char *s);

{
	if (*s == \'0')
	{	putchar (\'n);
	Return;
	_*putchar (*s);
	}
	_puts_recursion
}
